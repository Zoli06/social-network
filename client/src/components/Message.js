import React from 'react'

export const Message = ({ /*user: { firstName, lastName, middleName, userName, intro, profileImageMediaId }, group: { name }, createdAt, updatedAt, responses: { messageId }, reactions, reaction, upVotes, downVotes, vote, medias, */text }) => {
  return (
    <div>{text}</div>
  )
}